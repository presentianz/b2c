<!-- Hot Staffs -->

            <div class="hotdeal">
                <div class="row-up">
                    <div class="col-xs-11">
                        <h3>可能喜欢</h3>
                    </div>
                    <div class="col-xs-1">
                        <a href="products.html">更多
                        </a> 
                    </div>
                </div>
                <div class="row">
                {% for product in data %}
                    <div class="col-xs-3">
                        <a href="{{ path('product', { id : product.id }) }}">
                            <img class="lazy" src="{{ asset('img/default.png') }}" data-original="{{ asset(random(['img/aptami.jpg', 'img/honey.jpg', 'img/honey2.jpg' ,'img/blackmores.jpg', 'img/artemis.jpg', 'img/manukablendhoney.jpg', 'img/milkchews.jpg', 'img/royalnectar.jpg', 'img/swisse.jpg'])) }}" alt="">
                            <span class="pro-text"><span class="pro-title">{{ product.name }}<br></span><span class="price">${{ product.priceDiscounted }}</span><del>${{ product.price }}</del></span>
                        </a>
                    </div>
                {% endfor %}
                </div>	
            </div>